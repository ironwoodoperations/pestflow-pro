# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.*.local

# AI session context files
pestflow-pro-context-s*.md
*-context-session*.md

# Next.js
.next/

# Vite build output (now inside Next.js public folder)
public/_admin/
.vercel
.env-backups/
tsconfig.next.tsbuildinfo
tsconfig.tsbuildinfo
supabase/.temp/

# Claude Code local settings (per-developer, do not commit)
.claude/settings.local.json

# Prospect Teardown Engine artifacts (scored prospect output/videos/frames)
tools/teardown/output/

# S337 — the repo's first Python test lives in .github/scripts/. Running it
# locally writes bytecode next to the source; that is build output, not a
# change, and leaving it untracked trips the session git check.
__pycache__/
*.pyc
