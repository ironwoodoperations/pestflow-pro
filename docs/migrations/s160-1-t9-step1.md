# Migration s160.1 — T9 Step 1: Expand-phase code tolerance for branding.template → theme

Executed: 2026-04-20
Commit: 36cc44a

## Context

3-deploy strategy for renaming `branding.template` → `branding.theme` in the settings JSONB:
- **Step 1 (this):** Code reads both keys (`theme ?? template`); save handler dual-writes both
- **Step 2 (T9.2):** SQL migration renames `template` → `theme` in all `settings.branding` rows
- **Step 3 (T9.3):** CHECK constraint + remove fallback reads

## Files changed

| File | Change |
|------|--------|
| `src/components/admin/settings/BrandingSection.tsx:50` | Read: `((v as any).theme ?? v.template) ?? prev.template` |
| `src/components/admin/settings/BrandingSection.tsx:69` | Save: dual-writes `{ ...form, theme: form.template }` |
| `src/context/TenantBootProvider.tsx:19` | `RawBoot` interface: added `theme?: string` |
| `src/context/TenantBootProvider.tsx:22` | `mapRaw()`: `template: r.theme ?? r.template` |
| `src/components/ironwood/AllClients.tsx:50` | `r.value.theme ?? r.value.template` |
| `src/pages/admin/Dashboard.tsx:91` | `brandRes.data.value.theme ?? brandRes.data.value.template` |
| `src/pages/IntakeSuccess.tsx:69` | `(b.theme ?? b.template) \|\| 'modern-pro'` |

## Sites NOT changed (read from prospects.branding, not settings.branding)

- `IntakeSubmissionViewer.tsx` — reads `prospect.intake_data.branding` (intake form state)
- `ProspectDetail.Branding.tsx` — reads `prospect.branding` (pre-provision form state)
- `ProspectDetail.IntakeLink.tsx` — reads `prospect.branding` (merge logic)
- `PreProvisionChecklist.tsx` — reads `prospect.branding` (gate check)

## Verification

- TypeScript: CLEAN (tsc --noEmit, no errors)
- Build: CLEAN, bundle within limits
- All 5 settings.branding read sites patched with (theme ?? template) fallback

## Next: T9 Step 2

SQL migration to rename `template` → `theme` in all `settings.branding` JSONB rows.
Safe to run on any tenant count — all read paths are now tolerant of either key.
