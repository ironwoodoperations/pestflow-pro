You are working on PestFlow Pro — a multi-tenant SaaS platform for pest control businesses.
Model: claude-sonnet-4-6 (always, no exceptions)
Dev server: doppler run -- npm run dev
Start every session: git checkout main && git pull
Commit after every task: git add . && git commit -m "task[N]: description" && git push
All files must stay under 200 lines. Split into sub-components if needed.
Relative imports only — no @/ aliases.
Do NOT generate a context file at the end — plain summary only.
STOP and output a plain summary when you estimate you are at 50% context window.

---

GOAL: S91 — Replace Ayrshare with Buffer as the social media provider,
format phone numbers consistently, and do a final pre-launch browser walkthrough
of both the demo site and Cypress Creek before the first real client call.

---

TASK 1 — Replace Ayrshare with Buffer in the social admin tab

Background:
- active_social_provider is now 'buffer' in settings.integrations
- Buffer API endpoint: https://api.bufferapp.com/1/updates/create.json
- Buffer auth: per-user access token (OAuth2)
- Per-tenant token stored in: settings.integrations.buffer_access_token
- Platform-level token (for testing): BUFFER_ACCESS_TOKEN in Supabase edge function secrets

Find all files that reference Ayrshare in the admin social tab:
  grep -r "ayrshare" src/ --include="*.tsx" --include="*.ts" -i

For each file found:

a) If it's UI copy/labels referencing "Ayrshare":
   Replace display text with "Buffer"

b) If it's an API call to Ayrshare's endpoint:
   Replace with Buffer's API call pattern:
   POST https://api.bufferapp.com/1/updates/create.json
   Headers: { Authorization: Bearer {buffer_access_token} }
   Body: { text: postContent, profile_ids: [profileId] }
   profileId comes from settings.integrations.buffer_profile_ids (array — one per connected channel)

c) If it's a connect/disconnect flow:
   Replace Ayrshare OAuth with Buffer OAuth:
   Buffer OAuth authorize URL: https://bufferapp.com/oauth2/authorize
   Params: client_id, redirect_uri, response_type=code
   BUFFER_CLIENT_ID + BUFFER_CLIENT_SECRET in Doppler (frontend) and Supabase secrets (edge fn)
   For now: if Buffer OAuth is complex to wire fully, replace with a manual token
   input field in the admin social settings (rep pastes in Buffer access token directly).
   Label it "Buffer Access Token" with a link to buffer.com/developers

d) Update settings.integrations schema in any TypeScript types:
   Replace ayrshare_api_key / ayrshare_profile_id with:
   buffer_access_token: string
   buffer_profile_ids: string[] (array of connected channel profile IDs)

Keep the active_social_provider toggle — future support for Ayrshare at scale means
we should not hard-delete the Ayrshare path, just make Buffer the active default.
Comment out (do not delete) any Ayrshare-specific API call code.

---

TASK 2 — Phone number formatting utility

business_info.phone is stored inconsistently across tenants:
  - Demo: "903-541-9287"
  - Cypress Creek: "9035419287" (unformatted)

Create a small utility function: src/lib/formatPhone.ts

```typescript
export function formatPhone(raw: string): string {
  const digits = raw.replace(/\D/g, '')
  if (digits.length === 10) {
    return `(${digits.slice(0,3)}) ${digits.slice(3,6)}-${digits.slice(6)}`
  }
  if (digits.length === 11 && digits[0] === '1') {
    return `(${digits.slice(1,4)}) ${digits.slice(4,7)}-${digits.slice(7)}`
  }
  return raw // return original if unrecognized format
}
```

Apply formatPhone() wherever phone numbers are displayed in:
- Public shell components (navbar, footer, contact page, hero CTA button)
- Customer auto-reply email (already has phone in body — wrap with formatPhone)
- Admin business info display (read-only display only — not the input field itself)

Do NOT format the stored value in the DB — only format at display time.

---

TASK 3 — Pre-launch browser walkthrough

Start dev server: doppler run -- npm run dev

Walk through the live demo site at localhost and report pass/fail:

PUBLIC SITE — demo tenant (pestflow-pro.localhost or localhost with demo tenant):
[ ] Home page loads — modern-pro shell visible
[ ] Hero section renders — headline, CTA button
[ ] Services grid renders — pest service cards with images
[ ] Nav: Services dropdown opens, all links work
[ ] Footer: business name, phone (formatted), address visible
[ ] Quote form: fill out and submit — confirm lead appears in DB
[ ] Lead notification email: check Resend logs — confirm delivered not bounced
[ ] Customer auto-reply: check Resend logs — confirm delivered not bounced
[ ] About page loads
[ ] Contact page loads — phone number formatted correctly
[ ] Service page loads (spot check: /termite-control)

ADMIN DASHBOARD — login as admin@pestflowpro.com / pf123demo:
[ ] Dashboard loads — leads list, stats
[ ] Content tab: edit home page headline, save, confirm persists on refresh
[ ] Branding tab: palette picker, shell selector visible
[ ] Social tab: shows Buffer (not Ayrshare) — token input field visible
[ ] SEO tab: AI Generate button visible per page row
[ ] Blog tab: create draft post, confirm saves
[ ] Leads tab: test lead from quote form appears

IRONWOOD OPS — /ironwood:
[ ] Prospects list loads
[ ] Tier selector works on a prospect
[ ] Generate Pro Layout button appears on tier=pro prospect

Report any failures with exact symptom. Fix anything that is a quick fix (< 10 lines).
Log anything that requires a full task as "needs S92".

---

TASK 4 — Stripe live mode readiness check

Scott needs to manually flip Stripe to live mode before the first real charge.
This task is a code audit only — do not change any Stripe keys.

Check these files for any hardcoded test mode references:
  grep -r "sk_test\|pk_test\|test_" src/ supabase/functions/ --include="*.ts" --include="*.tsx"

Report every file and line that contains a test key reference.
Confirm that all Stripe keys are read from environment variables (Doppler/Supabase secrets)
and not hardcoded anywhere.

Check supabase/functions/stripe-webhook/index.ts:
  Confirm STRIPE_WEBHOOK_SECRET is read from Deno.env.get()
  Confirm the webhook handler is not filtering by test event types only

Report findings — do not change anything. Scott will swap keys manually.

---

VERIFICATION

1. grep -r "ayrshare" src/ returns zero results (or only commented-out code)
2. Buffer is shown in the admin social tab with token input
3. formatPhone() utility created and applied to display locations
4. Browser walkthrough complete — pass/fail reported for every item
5. Stripe audit complete — no hardcoded test keys found
6. 0 TS errors, bundle under 450 kB
7. git add . && git commit -m "S91: Buffer social, phone formatting, pre-launch walkthrough" && git push

Plain summary at end. No context file.
